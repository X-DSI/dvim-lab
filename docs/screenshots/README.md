# Screenshots

Images referenced by the project README.

| File | Shows |
|---|---|
| `dashboard.png` | The start screen — logo, version, menu, startup stats |
| `editor.png` | A working session — file tree, buffer tabs, diagnostics, terminal |

Keep these current when the dashboard or theme changes: a screenshot showing an old
version number is worse than no screenshot.
